#include <cmath>

namespace Engine2D{

    // Define 'real' as a float for single precision, allows for ability to change to double later if higher precision is needed
    typedef float real;
    #define real_sqrt sqrtf

}